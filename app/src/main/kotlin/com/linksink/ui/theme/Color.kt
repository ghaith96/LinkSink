package com.linksink.ui.theme

import androidx.compose.ui.graphics.Color

// --- Brand primaries ---
val DiscordBlue = Color(0xFF5865F2)
val DiscordBlueLight = Color(0xFF7983F5)
val DiscordBlueDark = Color(0xFF4752C4)

val LinkSinkPrimary = DiscordBlue
val LinkSinkPrimaryLight = DiscordBlueLight
val LinkSinkPrimaryDark = DiscordBlueDark

// --- Primary container (tinted surface for chips, highlighted rows) ---
val LinkSinkPrimaryContainer = Color(0xFFDFE1FF)       // light: soft indigo tint
val LinkSinkOnPrimaryContainer = Color(0xFF0008A1)     // dark text on light container
val LinkSinkPrimaryContainerDark = Color(0xFF323AA0)   // dark: deeper indigo container
val LinkSinkOnPrimaryContainerDark = Color(0xFFDFE1FF) // light text on dark container

// --- Secondary (muted blue-violet) ---
val LinkSinkSecondary = Color(0xFF5C5F8A)              // desaturated indigo-violet
val LinkSinkOnSecondary = Color(0xFFFFFFFF)
val LinkSinkSecondaryContainer = Color(0xFFE2E0FF)     // pale lavender
val LinkSinkOnSecondaryContainer = Color(0xFF181A4D)
val LinkSinkSecondaryDark = Color(0xFFC3C2FF)          // pastel lavender for dark
val LinkSinkOnSecondaryDark = Color(0xFF2B2D61)
val LinkSinkSecondaryContainerDark = Color(0xFF434571)
val LinkSinkOnSecondaryContainerDark = Color(0xFFE2E0FF)

// --- Tertiary (warm teal accent) ---
val LinkSinkTertiary = Color(0xFF006874)               // teal
val LinkSinkOnTertiary = Color(0xFFFFFFFF)
val LinkSinkTertiaryContainer = Color(0xFF9EEFFD)      // pale cyan
val LinkSinkOnTertiaryContainer = Color(0xFF001F24)
val LinkSinkTertiaryDark = Color(0xFF4FD8EB)           // bright teal for dark
val LinkSinkOnTertiaryDark = Color(0xFF00363D)
val LinkSinkTertiaryContainerDark = Color(0xFF004F58)
val LinkSinkOnTertiaryContainerDark = Color(0xFF9EEFFD)

// --- Surface / background (blue-tinted neutrals) ---
val LinkSinkBackground = Color(0xFFFBFBFF)
val LinkSinkOnBackground = Color(0xFF1B1B1F)
val LinkSinkSurface = Color(0xFFFBFBFF)
val LinkSinkOnSurface = Color(0xFF1B1B1F)
val LinkSinkSurfaceVariant = Color(0xFFE3E1EC)
val LinkSinkOnSurfaceVariant = Color(0xFF46464F)
val LinkSinkOutline = Color(0xFF767680)
val LinkSinkOutlineVariant = Color(0xFFC7C5D0)

val LinkSinkBackgroundDark = Color(0xFF1B1B1F)
val LinkSinkOnBackgroundDark = Color(0xFFE4E1E6)
val LinkSinkSurfaceDark = Color(0xFF1B1B1F)
val LinkSinkOnSurfaceDark = Color(0xFFE4E1E6)
val LinkSinkSurfaceVariantDark = Color(0xFF46464F)
val LinkSinkOnSurfaceVariantDark = Color(0xFFC7C5D0)
val LinkSinkOutlineDark = Color(0xFF90909A)
val LinkSinkOutlineVariantDark = Color(0xFF46464F)
